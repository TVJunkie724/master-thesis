# Deployment Manifest v4

Manifest v4 packages one Six-layer RTA v2 and RDS v2 together with provider
packages, compatibility references, and an empty credential envelope.
Deployment readiness is enforced by the Management API and Deployer.

Generated service copies are synchronized by
`scripts/sync_six_layer_contracts.py`.
