# Cloud Setup

This section will hold canonical setup guidance for AWS, Azure, and GCP.

Planned content:

- cloud account prerequisites,
- one-time bootstrap flows,
- least-privilege deployment identities,
- local development credential handling,
- Docker Compose profiles for default, development, demo, and cloud-integration modes.

Real credential values must never be committed or pasted into documentation. Examples should use placeholders or schema-only files.
