schema_version: pricing-registry-service-models.v1
registry_version: "2026.07.17"
service_models:
  cost_model_v1:
    metric: cost
    description: Current thesis cost calculation model.
    intent_groups: [cost]
    intents:
      - transfer.egress_gb
      - iot.message_ingest
      - functions.request
      - functions.compute_gb_second
      - storage.hot.storage_gb_month
      - storage.hot.read_request
      - storage.hot.write_request
      - storage.cool.storage_gb_month
      - storage.archive.storage_gb_month
      - storage.archive.write_request
      - api.request_million
      - orchestration.state_transition
      - event_bus.event_million
      - digital_twin.operation
      - digital_twin.message
      - digital_twin.entity_month
      - digital_twin.api_call
      - digital_twin.query
      - digital_twin.query_unit
      - digital_twin.account_bundle_month
      - grafana.editor_user_month
      - grafana.viewer_user_month
    notes:
      - AWS IoT TwinMaker, IoT Hub, transfer tiers, and storage operation tiers require provider-specific review before formula changes.
    azure_calculation_model:
      status: reviewed_for_phase_17
      issue: 114
      assumptions:
        - IoT Hub is modeled as monthly unit-tier pricing. Free tier applies up to its documented monthly message limit; paid tiers use the cheapest valid tier based on included messages per unit and maximum tier limit.
        - Azure Digital Twins operation, routed-message, and query-unit meters are separate usage dimensions normalized from the Retail Prices API 1K billing block.
        - Every telemetry update produces one ADT operation in five-layer-baseline@1; each logical query adds response-size-based 1 KB operation units and an explicit estimated query-unit weight.
        - five-layer-baseline@1 deploys no ADT outbound Event Route, so its routed-message quantity is exactly zero.
        - The canonical L4 execution component is the ADT Pusher function; the L1 IoT Hub to Event Grid path is priced only in L1.
        - Blob Storage read/write operation meters are normalized before multiplication; per-10K request keys are converted to one request.
        - Logic Apps and Event Grid action/event meters are normalized from provider billing blocks before multiplication.
        - Azure transfer/egress uses cumulative pricing tiers when tier data is available.
      open_research:
        - Verify Blob Storage operation and retrieval meters per replication/region/SKU before final cross-provider validation.
        - API Management is represented in the pricing registry, but not yet part of the executable layer model.
    aws_calculation_model:
      status: reviewed_for_phase_18
      issue: 115
      assumptions:
        - IoT Core message pricing is cumulative tier pricing over billable messages. Message size uses AWS 5KB billing increments before tier calculation.
        - IoT Core tier prices must be normalized to one billable message. Tiers may provide explicit `pricePerMessage`, normalized `price`, or `pricePerMillionMessages`.
        - S3 Infrequent Access and Glacier request/lifecycle prices are normalized before multiplication; per-1K and per-10K request keys are supported.
        - IoT TwinMaker Standard entities, unified data access API calls, and queries are calculated as separate provider-API dimensions.
        - IoT TwinMaker Tiered Bundle is an account-wide composite schedule and is never inferred as a per-twin price.
        - A fresh account-scoped GetPricingPlan observation gates whether Basic, Standard, or Tiered Bundle semantics are applicable.
        - Step Functions and EventBridge action/event prices are normalized from explicit per-unit or true per-million/per-1K fields.
        - AWS transfer/egress uses cumulative pricing tiers when tier data is available.
      open_research:
        - Finalize account-wide Tiered Bundle allocation only when explicit aggregate account usage and an approved allocation policy are available.
        - Verify Managed Grafana editor/viewer user pricing against current AWS evidence; missing evidence remains review-required.
        - API Gateway and Scheduler are represented in the pricing registry, but not yet part of the executable layer model.
    gcp_pricing_evidence_model:
      status: reviewed_for_phase_9
      issue: 93
      required_catalog_operations:
        - operation: cloudbilling.services.list
          permission: cloudbilling.services.list
        - operation: cloudbilling.skus.list
          permission: cloudbilling.skus.list
      assumptions:
        - GCP Billing Catalog access must pass preflight before GCP Catalog evidence can be treated as live-valid.
        - Credential/auth failures are structured with operation and permission context and must redact service-account private keys, private key IDs, client emails, and tokens.
        - GCP Catalog SKUs are converted into selected/candidate/rejected evidence records with service, SKU, category, region, unit, pricing expression, and tiered-rate details.
        - GCP fallback pricing remains calculation-compatible only as review-required emergency data.
      open_research:
        - Exact Catalog mappings for Pub/Sub volume-based IoT equivalent, Firestore operations, Cloud Storage classes, Workflows, API Gateway/Service Control, and Compute Engine self-hosted equivalents are the input to Phase 11.
        - Final GCP formula/tiering changes are planned in issue 95 and must not be inferred from the evidence phase alone.
    gcp_calculation_model:
      status: reviewed_for_phase_11
      issue: 95
      assumptions:
        - GCP calculation hardening starts from Billing Catalog evidence and official Google Cloud pricing documentation; existing fallback values remain emergency-only diagnostics.
        - Pub/Sub throughput, storage, transfer, and request-size minimum behavior must be normalized separately when the thesis workload input exposes the required dimensions.
        - Firestore reads, writes, deletes, storage, index-entry reads, named-database behavior, and free-tier applicability must be represented only where supported by evidence and executable workload inputs.
        - Cloud Storage storage class, operation class, retrieval, minimum-duration, and early-deletion assumptions must be documented before formula changes are made.
        - Workflows internal and external steps must be calculated separately with official increment/free-tier semantics where supported.
        - Cloud Run functions pricing must be mapped to the current Cloud Run-backed pricing model, or missing dimensions must remain review-required.
        - Compute Engine self-hosted equivalents must separate machine-price assumptions from disk/storage assumptions.
        - GCP transfer pricing must distinguish route and network-tier assumptions where the optimizer intent can express them.
        - Missing required supported GCP pricing fields fail visibly instead of producing silent zero cost or hidden defaults.
      open_research:
        - Live cloud deployment E2E remains out of scope until the remaining architecture refactorings are complete.
        - Custom contracts, committed-use discounts, sustained-use discounts, Spot/preemptible pricing, and non-modeled regions remain out of scope unless explicit evidence is added.
    cross_provider_validation_model:
      status: implemented_for_phase_10
      issue: 94
      validation_schema_version: cross-provider-cost-validation.v1
      required_profile: cost_minimization_v1
      required_result_schema: cost-result.v1
      rules:
        - Every active cost intent must have AWS, Azure, and GCP evidence or explicit non-applicability.
        - Publishable validation rejects missing provider reports, missing intent evidence, review-required evidence, fallback_static, incompatible normalized units, and incompatible optimization profile metadata.
        - Explicit non-applicability is acceptable only when it is represented as provider evidence and is not review-required.
        - Calculation results must expose evidenceReferences so the Management API run store can persist the audit trail in result_summary_json.
      output:
        - Structured provider/intent status for future UI diagnostics.
        - Deterministic selected evidence ids per provider/intent.
        - Provider selected-row identity plus candidate and rejected counts.
        - Management-run compatibility status for existing CostCalculationRun persistence.
      open_research:
        - Live provider E2E remains deferred until the remaining architecture refactorings are complete.
        - Provider-specific pricing tiers and service equivalence must still be verified with final live evidence before thesis evaluation claims are frozen.
