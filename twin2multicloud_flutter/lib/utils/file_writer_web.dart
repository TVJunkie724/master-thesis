Future<void> writeBytesToPath(String path, List<int> bytes) {
  throw UnsupportedError('Direct path writes are unavailable on Web.');
}

Future<void> writeTextToPath(String path, String content) {
  throw UnsupportedError('Direct path writes are unavailable on Web.');
}
